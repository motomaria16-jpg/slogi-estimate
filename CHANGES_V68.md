# SLOGI v68

- Исправлен скролл главной страницы: убран wheel-lock из-за `overscroll-behavior: contain`, сохранён горизонтальный скролл широкой таблицы и вертикальный скролл списка до раскрытия.
- Карточка объекта переведена на устойчивую flex-модель: фиксированные шапка/футер + единственный прокручиваемый body с `min-height: 0`.
- Конкурентный анализ переведён на flex-модель с отдельным скроллом таблицы.
- Убраны фиксированные `top: 48px` для второй строки заголовка конкурентного анализа.
- Весь `thead` теперь sticky целиком, поэтому высота первой строки вычисляется браузером и длинные названия столбцов больше не наезжают на вторую строку.
