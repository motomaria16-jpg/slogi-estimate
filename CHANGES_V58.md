# v58

- карточка Добавить объект стала крупным центрированным modal, а не правым drawer;
- широта/долгота скрыты из интерфейса, координаты остаются внутренними служебными данными;
- при изменении адреса старые координаты/кластер сбрасываются, запускается новое геокодирование и автоматическое определение кластера;
- карта показывает подпись адреса рядом с каждой точкой; выбранный объект выделяется;
- добавлен серверный контур импорта ЦИАН/Авито через Supabase Edge Function;
- подготовлен Browserless Smart Scrape fallback для страниц с anti-bot;
- обновлены cache-busting версии главной страницы.
